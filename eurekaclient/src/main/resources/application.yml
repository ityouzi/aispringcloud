server:
  port: 8011
spring:
  application:
    name: provider              #当前服务注册在Eureka Server上的名字
eureka:
  client:
    service-url:                #注册中心访问地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true     #是否将当前服务的IP注册到Eureka Server